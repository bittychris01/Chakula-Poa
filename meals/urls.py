# meals/urls.py
from django.urls import path
from . import views  # make sure you have a views.py in meals

urlpatterns = [
    # Example endpoints — replace with your actual views
    # path('', views.meal_list, name='meal-list'),
    # path('<int:id>/', views.meal_detail, name='meal-detail'),
]
